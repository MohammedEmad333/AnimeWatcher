{
    "name": "animewatcher/backend",
    "description": "Plain-PHP + MySQL backend for the AnimeWatcher app (JWT auth).",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": ">=8.1"
    },
    "suggest": {
        "firebase/php-jwt": "Optional drop-in replacement for the built-in src/Jwt.php handler."
    },
    "autoload": {
        "classmap": ["src/"]
    }
}
